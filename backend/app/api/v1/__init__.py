"""Reserved for the api epic (Build sequencing §10.C, ARCHITECTURE.md).

api/v1: the public, versioned API surface — thin, curated adapters over
module services, decoupled from internal routers so internal refactors
don't break external consumers. Empty until that epic lands.
"""
